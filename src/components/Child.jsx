// const Child = (props) => {
//   return <p>{props.name}</p>;
// };

// export default Child;

const Child = ({ name }) => {
  //in line deconstructing
  return <p>{name}</p>;
};

export default Child;
